# 《问道长生》项目分析报告

> 分析日期：2025-08-30 ｜ 分析范围：`wendao-changsheng/`（src 41 文件 ≈ 8185 行）+ 工程/构建/发布配置 + 对照设计文档《修仙模拟器.docx》（已提取全文，21 章 + 附录）
> 方法：双线审计（代码/架构线 + 工程/构建线），关键 P0 结论已人工抽查复核；未修改任何项目文件。
> 结论摘要：**架构意识高于平均水准的中型游戏项目**，主要问题集中在三类——①跨模块"表示漂移"（injury id/中文名、`'炼丹'`/`'lian-dan'`、`flags` 魔法键）；②几处核心数值/流程硬伤（衰老结算失效、修炼公式 10× 通胀、逆天改命死链、突破三途无差异）；③工程卫生（80MB PNG 入库、release 不可发布、零 CI/lint）。

---

## 0. 决策前提（本次分析已与作者确认）

| 决策 | 结论 |
|---|---|
| 产出 | 评估报告 + 按优先级分级的待办清单（附文件定位） |
| 优先级 | 玩法与数值正确性 > 架构与代码质量 > 性能与流畅度（Android 打包/测试暂缓） |
| 改动幅度 | 低成本快速赢优先 |
| LLM 定位 | LLM 是核心叙事引擎，只做"诚实的降级体验"（修文案 + 无 Key 时系统指令可玩），不实现完整离线模式 |
| 文档对齐 | 核心体验以设计文档为准，细节双向对齐（文档过时处允许修订） |
| 存档兼容 | 无需兼容旧档，但修复时给存档加版本号留迁移口子 |
| 建议深度 | P0 给具体改法（改动点/怎么改/怎么验证）；P1/P2 给方向 + 文件定位 |

---

## 1. 项目现状总览

### 1.1 技术栈与结构

| 项 | 结论 |
|---|---|
| 框架 | React 19 + TypeScript（`tsc -b` 严格编译） |
| 构建 | Vite 8（`base: './'` 兼容 Capacitor 安卓 WebView 相对路径） |
| 状态 | Zustand 5（`persist` 只持久化 settings；游戏进度走独立 localStorage 键） |
| 样式 | Tailwind CSS v4 + 自研"宣纸设计系统"（index.css 设计令牌，复刻原文 4.5 配色） |
| 关键依赖 | zustand、react、tailwindcss、vite、@capacitor/*、playwright/tsx/sharp（测试与素材脚本） |
| 测试 | 无框架；自研 `scripts/smoke.ts`（31 断言）+ `scripts/e2e.ts`（Playwright）+ `test-validate.ts`（30 断言） |

```
src/
├── main.tsx / App.tsx          # 入口 + 屏幕路由 + 会话恢复/多标签接管
├── game/
│   ├── state.ts                # GameState/SaveFile 类型（125 行）
│   ├── store.ts                # Zustand store：回合执行、会话持久化、快照回退（344 行）
│   ├── save.ts / session.ts    # 3 槽存档+自动+快照+JSON 导入导出
│   ├── data/                   # 内容数据（全部标注原文章节，纯数据无逻辑）
│   ├── engine/                 # 纯函数数值引擎（零 React 依赖，不可变更新）
│   │   ├── turn.ts(694) / actions.ts(546) / cultivation.ts(305)
│   │   ├── breakthrough.ts(417) / combat.ts(748) / economy.ts(336) / systems.ts(605)
│   │   └── offline.ts          # ⚠ 死代码（从未被 import）
│   └── narrator/               # llm.ts（OpenAI 兼容客户端+JSON 协议+系统提示词）、balance.ts
└── ui/                         # 宣纸设计系统 + 各屏（CreationWizard 426 行等）
```

### 1.2 核心玩法循环

玩家输入 → `store.submitAction`（store.ts:268）→ `resolveTurn`（turn.ts:537）：`routeCommand` 判定命令 → **系统指令走代码结算**（修炼/突破/坊市/战斗），**自由行动走 LLM**（强制 JSON）→ AI 的 `proposedStateChanges` 经 `validateProposedStateChanges`（turn.ts:134）语义校验、`applyDeltas`（turn.ts:297）钳制落地 → 时间唯一来源 `timePassedMonths`（AI 返回，钳 0~12）→ 衰老结算（turn.ts:510）→ 写现场会话、每 30 回合自动存档。

**设计定调（README 明示）：代码是数值的唯一权威，LLM 只负责叙事与选项**——这是本项目最核心、也执行得最扎实的架构决策。

### 1.3 构建链现状（实测）

- **Web 构建 ✅**：`tsc -b --force` 0 错误；`vite build` 通过；`npm run smoke` 31/31。
- **Android 链路 ⚠️**：环境齐全（JDK21 / SDK android-36 / gradle 缓存），8/24 曾成功产出 `app-debug.apk`（87.5MB）；但 **assets 与 APK 落后当前代码 ≥1 commit**（assets 内是 `index-HLd9bIbv.js`，dist 是 8/30 的 `index-Dw_pZ_ps.js`），现存 APK 是旧版。
- 安全核查：所有被跟踪文件 grep 密钥零命中；node_modules/dist/local.properties/assets 忽略正确；无 .DS_Store/密钥入库。

---

## 2. 问题清单（按严重度分级）

### P0 · 必须修（核心机制失效 / 仓库硬伤）

**P0-1 【玩法】衰老/寿元结算失效——"寿元倒计时"核心机制不工作**
`turn.ts:510-526` `applyAging`：`years = Math.floor(acc/12)`，其中 `acc = flags.ageMonths + 本月流逝`，而 `ageMonths = acc - Math.floor(acc)` **只保留小数余数（恒 <1）**，整月全部被推入时间线（turn.ts:652-654）却从不累计进年龄。结果：
- 正常 1 月/回合游玩 12 个月后 `timeline.year` 正常 +1，但 `player.age`、`res.lifespan` 纹丝不动；
- 只有单回合流逝 ≥12 月（如"闭关 1 年"）才 +1 岁——**衰老行为依赖玩家是否批量闭关**；
- `坐化` 死亡常规游玩永不触发，寿元紧张感（原文核心设定）名存实亡。
（已复核：注释声称"避免年龄涨了年份不动"，实现与注释意图相反。）

**P0-2 【玩法】`res.injury` 表示混用（id vs 中文名）——多个子系统连锁失效**
写入点各写各的：
- 存 id：`breakthrough.ts:105`（`'severe'`）、`:140,160`（`'dying'`）、`:409`（`'heart-demon'`）
- 存中文名：`systems.ts:261`（`injuryName('light')`→'轻伤'）、`:427`（→'重伤'）、`:439`（→'心魔缠身'）、`economy.ts:316`（`'轻伤'`）

读取点各按各的判：
- 按 id：`cultivation.ts:169,174`、`breakthrough.ts:90,233,388`
- 按中文：`combat.ts:316-317`（`inj.includes('垂死')` 拿 id 匹配中文——**战斗重伤/垂死压制永不生效**）

连锁后果：大突破失败留下的重伤（id 存储）无法被 闭关疗伤/寻医/丹药 治愈（`heal()` 在 systems.ts:555 用 `injuryIdOf` 只认中文名 → 报"未知伤势"）；中文名存储的心魔/内伤不触发闭关效率暴跌、渡劫 −10%、渡劫清心魔等任何惩罚。这是最典型的"两套表示漂移"型 bug。（已复核全部写入/读取点。）

**P0-3 【玩法】修炼公式 10× 通胀——成长曲线整体崩坏**
`cultivation.ts:191-193`：`gain = 10 * zizhi * root * ... * growth * ...`。原文 9.1 公式"月修为 = **10** × 资质 × 灵根 × 功法 × 灵气 × 心境"中的 **10 就是炼气月修为基数**（9.1b：炼气 10 → 筑基 9 → 逐境递减），代码把字面量 `10` 与 `growth`（`GROWTH_BASE`，炼气=10）**重复相乘** = 恒定放大 10 倍。默认配置炼气单月修为 ≈ 195 > cultMax 100 → **每月自动小突破一次**；对照原文"炼气 3~10 年"，实测约 1 年即到炼气圆满。`smoke.ts` 只断言 `gained > 0` 不校验量级，故测试通过。（已复核。）

**P0-4 【工程】80MB 背景图入库，仓库 93.7MB 且已推送**
`public/bg/*.png` 共 36 个文件约 80MB 被 git 跟踪，`.git` 已达 93.68 MiB 并推送到 `github.com/molixx/wendao-changsheng`，无 LFS。任何协作者 clone 都要拖 90+MB。（已复核 `git count-objects`。）

**P0-5 【工程】release 不可发布**
无 signingConfig / keystore / 版本号管理（versionCode 固定 1）；`build-apk.sh` 硬编码 4 条 `/Users/asura` 路径，仅本机可用。（用户本次暂缓 Android 打包，但此项应至少完成"版本号参数化"，见第 3 节第 4 档。）

### P1 · 设计缺陷 / 重大体验问题

- **P1-1 逆天改命死链**：`breakthrough.ts:246` 突破成功后提示"请调用 applyFateChange"——但全库 grep 不到任何调用方，玩家永远选不了 12 项天赋，"突破成功奖励"形同虚设。
- **P1-2 无 Key 完全不可玩，与文案矛盾**：`turn.ts:561,607` 在 `!useLlm` 时直接 throw → store.ts:303-314 转 turnError，一步都动不了；而 `SettingsPanel.tsx:129-131` 与 README 均宣称"无 Key 自动降级离线叙事，游戏不会卡死"——与实现不符；`offline.ts` 整个文件是死代码。`e2e.ts:1` 注释证实"要求联网叙事"是刻意决策，**是文案/UI 未同步更新**。
- **P1-3 突破三途与原文不符**：`breakthrough.ts:180-249` 人道/地道/天道**只差成功率**，不校验/消耗任何资源（原文 8.2：人道需突破丹药、地道需天材地宝、天道需灵珠道韵+雷劫），成功后无属性/收益差异（原文"天道筑基远超人道筑基"）。坊市"筑基丹"（economy.ts:46-48 明示"突破时消耗，可显著提高成功率"）在 `majorBreakthrough` 里从未被读取。
- **P1-4 数据键不一致 `'炼丹'` vs `'lian-dan'`**：`CreationWizard.tsx:88` 天赋"药理通神"写入 `techniqueLevels['炼丹']=1`（中文键），引擎统一用 id `'lian-dan'`（systems.ts:114、breakthrough.ts:275）→ 该天赋实际无效；StatusCard 因"未命中 id 原样显示 key"掩盖了问题。同类：AI deltas 的 `bag` 中文键（"聚气丹"）直接进 `state.bag`（turn.ts:388-404），而 `useItem`/`marketBuy` 只认 id → AI 赠的物品"看得见用不了"。
- **P1-5 工程卫生**：零 CI、零 lint、无测试框架（`npm test` 不存在）；`scripts/` 15 个文件仅 8 个挂进 package.json（test-validate.ts、e2e-*.mjs、build-apk.sh 无入口）；文档漂移——README L50 / BACKGROUND-PROMPTS L3 称"PNG 优先"，代码实际 WebP→PNG→SVG（Background.tsx:37-38）；gradle wrapper 用腾讯镜像；e2e 依赖 msedge channel + DS_KEY。
- **P1-6 Android 测试模板坏件**：`android/app/src/{test,androidTest}` 包名仍是模板 `com.getcapacitor.myapp`，instrumented 测试断言 `com.getcapacitor.app` 必挂。（暂缓档。）

### P2 · 体验 / 细节缺陷

- **P2-1 `travelLike` 正则缺"探索/秘境"**：`turn.ts:144` 无"探索"二字，输入"探索南疆"时 LLM 提议的 location 变更被拒（turn.ts:158-162），而"游历"在表内，行为不一致。
- **P2-2 声望 bonus 被静默丢弃**：`CreationWizard.tsx:76-90` 官宦子弟 `bonus:{声望:20}`（creation.ts:15）不在 `STAT_BONUS_MAP` → 零加成；"妖族后裔"rare 标记无玩法接入。
- **P2-3 商品无闭环**：筑基丹/玄地阶功法/低级符箓无 `effect`（economy.ts:41-89）→ `useItem` 报"无需服用"；买到的功法**永远学不了**（GONGFAS 只能靠奇遇进 `gongfaIds`）；"摆摊卖货"路由回 `market`（actions.ts:71-75），`marketSell` 是死代码 → 无法出售。
- **P2-4 战斗丹药与背包脱节**：`combat.ts:565-583` 战斗中"用符/用丹"直接回血回灵，**不校验背包、不消耗物品**，与 economy 背包体系两套账。
- **P2-5 战斗压制机制永不触发**：`actions.ts:472` 遭遇战敌方 `realmIdx = max(0, 玩家−0或1)` 永不高过玩家 → 境界压制（×0.4/×0.1）与 REALM_PRESSURE 提示是摆设；受伤压制（combat.ts:316-317）因 P0-2 不生效；战斗失利一律 `flags.dead='战死'`（combat.ts:727），无原文 16.4 的"重伤昏迷"分支。
- **P2-6 存档导入跳过归一化**：`SavePanel.tsx:64-77,104-125` 读档/导入直接 `useGame.setState`，跳过 `normalizeLog`/`reconcileLifespan`（与 store.continueFromSave 不一致），旧档导入可能带回 id 冲突或寿元越界。
- **P2-7 性能隐患**：
  - 每回合全量序列化写 localStorage：`persistCurrentSession`（store.ts:82）把完整 GameState + 200 条 log（每条 1~3KB）写入 `wdcd.session`，单次数百 KB；5MB 配额约 10~20 回合告满，写入失败仅 console.error（session.ts:24-26）。
  - Zustand 无选择器订阅：UI 组件全量 `useGame()`，每回合 3~4 次 `set` 触发全部订阅者重渲染（组件树小，影响有限但可避免）。
  - StoryLog 历史弹窗一次渲染 200 条 SummaryRow 无虚拟化（StoryLog.tsx:241）；战斗日志整段塞进 `flags.combat` JSON 并持久化（combat.ts:689），长战斗膨胀。
- **P2-8 小工程项**：`@capacitor/cli/android` 放 dependencies；scripts/*.ts 与 capacitor.config.ts 无类型检查；`.npm-cache` 335MB 在项目内；无 `engines` 字段；`allowBackup=true` 未收紧。

### P3 · 小问题

- `StoryLog.tsx:170` 死三元（`offline ? 'panel--warn' : 'panel--warn'` 两分支相同）。
- `CreationWizard` 年龄输入清空得 0（绕过 min=16）；`canConfirm` 不校验天赋点是否超支。
- 作者署名与文档铁律不符：原文要求创角面板标题带"作者：雾见川"，实现为邮箱 + 确认页致谢（CreationWizard.tsx:198,373）。
- `B5` `turn.ts:318` `if (mode==='status') break` 用 break 跳数值循环，合法但脆弱。
- `B7` `StoryLog.tsx:71` `const vp: any = v as any` 可轻易类型化。

### 架构问题（跨模块系统性，建议中期处理）

- **B1 `flags` 是无类型垃圾桶**（state.ts:88 `Record<string,string|number|boolean>`）：combat 整段 JSON、ageMonths、breakCooldown、fate:*、heartStoneUsed、hiddenInjury、modao、spiritBeast、secretRealmOpen 等十几个魔法键无编译期保护——**P0-2/P1-4 这类"键表示漂移"事故的根源**。
- **B2 两套 deltas 校验逻辑重复且语义不一致**：`validateProposedStateChanges`（turn.ts:134-292，绝对值钳制）与 `applyDeltas`（turn.ts:297-496，增量语义）各自维护别名表/钳制/归一化（mood 归一化出现两次 turn.ts:206-213 与 431-440；stats 钳制 252 与 361 各一次且语义不同），合计 ~360 行。
- **B3 双份常量表手工同步**：`STAGE_COST_BASE` 在 cultivation.ts:61-72 与 breakthrough.ts:31-42 各一份（注释承认"按约束两文件互不 import"），改一处忘另一处即 cultMax 不一致。
- **B4 死代码**：`offline.ts` 整文件、`marketSell`/`earnSpirit`/`robbery`（economy.ts:183/274/292）、`daoHeartCheck`（breakthrough.ts:384）、`applyFateChange`/`availableFateChanges`；数据层 `BREAKTHROUGH_RATES`（realms.ts:36）、`STAGE_COST`（realms.ts:64）、`CULTIVATION_FORMULA`（systems.ts:121）无消费方，`MAJOR_EVENTS`/`ENDINGS` 仅被图鉴消费。
- **B6 魔法数字分散**：洞府升级价 `[200,800,3000,12000]`（systems.ts:175）、战利品 `(realmIdx+1)*30+rand(40)`（combat.ts:716 直接用 `Math.random` 而非 dice 工具）、遁走 `30+速差×5−境界×10`（combat.ts:282）——可接受但不利于平衡调参。

### 玩法/数值隐患（与原文对照）

- 修炼 10× 通胀（P0-3）+ 小突破成功率 ~40% 且逐月自动重试 → 炼气几乎无卡关，数值压迫感缺失。
- 逆天改命死链（P1-1）+ 三途无差异（P1-3）→ 突破正反馈被掏空。
- 心魔（daoHeartCheck 死代码）、暗伤（hiddenInjury 只写 flag 不结算）、内伤惩罚（P0-2）等"异常系统"大面积悬空，原文 7.1 受伤表只实现气血伤链条。
- 经济失衡：战斗战利品 30~70 灵石/场（同阶必赢）、宗门任务 21~50/次、洞府升级 200~12000——战斗是唯一高效收入源，卖货死代码使"倒卖"闭环缺失。
- 与原文明显缺失的系统：双修（多处提及零实现）、拍卖会/跑商、秘境探索（`secretRealmOpen` 置了 flag 无入口）、灵根系别选择（战斗硬编码 火/火木/火木水，combat.ts:113-118）、功法研习/装备（1 主 2 辅）、技艺产出（炼丹只升级不产药）、宗门大比/升仙大会/灵气潮汐（仅图鉴）、结局判定（ENDINGS 仅图鉴）、轮回转世、仙姿/声望数值效果。

---

## 3. 待办清单（按优先级分级）

> 排序依据：作者确认的优先级（玩法数值 > 架构质量 > 性能）；P0 给具体改法 + 验证方式，P1/P2 给方向与定位。
> 建议推进顺序：**第 1 档 4 项 → 第 2 档 → 第 3 档（择机）**，其中 P0-1→P0-2→P0-3 互不依赖可并行。

### 第 1 档 · 核心玩法修复（P0，均有具体改法）

**1. 修衰老结算（P0-1）— `turn.ts:510-526`**
- 改法：`acc` 改为累计全部月数，整年扣掉后保留余数——`const acc = ageMonths + months; const years = Math.floor(acc/12); const ageMonths = acc - years*12;`（删掉 `acc - Math.floor(acc)` 那行）。`applyAging` 的调用方（turn.ts:652-654 时间线推进）已传入相同 `months`，保持单一口径。
- 验证：`smoke.ts` 加断言——连续 12 次 `applyAging(state,1)` 后 `player.age +1` 且 `res.lifespan -1`；再断言 13 次后余数正确。

**2. 统一 injury 表示为 id（P0-2）**
- 改法：
  - 写入点全部改 id：`systems.ts:261` → `'light'`、`:427` → `'severe'`、`:439` → `'heart-demon'`、`economy.ts:316` → `'light'`；
  - 读取点统一 id 判断：`combat.ts:316-317` 改 `['dying','severe'].includes(injury)`（替换 `includes('垂死')/includes('重伤')`）；
  - `heal()`（systems.ts:555）的 `injuryIdOf` 加 id 兜底：先按 id 命中，再按 name/别名（复用 turn.ts:412 的 `INJURIES.find` 归一化逻辑）；
  - 顺带在 `data/systems.ts` INJURIES 表上写注释"**一律存 id，禁止中文名**"。
- 验证：smoke 加三断言——重伤状态下闭关效率（cultivation.ts:169 链）、战斗压制（combat.ts:316）、`heal()` 可治愈 id 型伤势。

**3. 修修炼公式（P0-3）— `cultivation.ts:191-193`**
- 改法：去掉字面量 `10`（保留 `growth`，炼气=10 由 `GROWTH_BASE` 提供），或去掉 `growth` 保留 10——二选一，别两个都留；随后按原文节奏（炼气 3~10 年）反推目标月修为量级，调整 `GROWTH_BASE` 或 `cultMax` 校准（对照原文 9.1b 逐境递减）。
- 验证：`smoke.ts` 加"炼气默认六维月修为 ∈ 合理区间（如 1~15）"断言防回归；`test-validate.ts` 同步。

**4. 接线逆天改命（P1-1）— 死代码复活**
- 改法：`actions.ts` 的 breakthrough 分支，突破成功且 `flags['fate:*']` 未设置时返回 `pendingOptions`（三选一，来自 `availableFateChanges`/数据），玩家选择后路由到 `applyFateChange`（breakthrough.ts:275 已实现，写 `flags.fate:*`）；顺带 `heartStoneUsed`/`心如磐石`（breakthrough.ts:213 已引用）即生效。
- 验证：smoke 断言"突破成功 → 出现 fate 选项 → 选择后 flags 写入"。

### 第 2 档 · 低成本快速赢

**5. LLM 诚实降级（P1-2，按作者决策：不做完整离线）**
- `turn.ts:561,607` 抛错改为：有 Key 走 LLM；无 Key 时**系统指令仍走代码结算**（executeSystem 不依赖 LLM 的部分），自由行动返回明确提示"需在设置中配置叙事引擎"——不再死锁。
- `SettingsPanel.tsx:129-131` 文案改为"未配置 Key 时，系统指令（修炼/突破/战斗/坊市）可玩，自由行动与叙事需配置"；README 同步。
- `offline.ts` 保留但文件头标注"未接入"；或直接删除（低成本推荐删除，避免误导）。
- 验证：无 Key 环境跑 `npm run smoke` 全绿 + 手工试玩系统指令。

**6. 对齐数据键（P1-4）**
- `CreationWizard.tsx:88` `'炼丹'` → `'lian-dan'`；`applyDeltas`（turn.ts:388-404）的 bag 键经物品 id/名反查映射后再写入；StatusCard 对未命中 key 显示"未知"而不是原样透传。

**7. 补商品闭环最小版（P2-3/P2-4）**
- 筑基丹：`majorBreakthrough` 人道路径校验并消耗、+成功率（原文 8.2）；
- 功法：买到的功法提供"研习/参悟"入口（悟性判定，对应原文 9.4）进入 `gongfaIds`；
- "摆摊卖货"路由到 `marketSell`（已实现，接线即可）；
- 战斗丹药（combat.ts:565-583）改为校验背包并消耗。

**8. 细节快速赢**
- `travelLike` 正则（turn.ts:144）加"探索|探秘"；
- `{声望:20}`（creation.ts:15）落地到 SectInfo/flags 并展示，或删掉（二选一）；"妖族后裔"rare 标记至少展示出来；
- `SavePanel` 读档/导入改走 `store.continueFromSave` 的归一化路径（normalizeLog/reconcileLifespan）；
- 存档 schema 升 v3（bump 版本号，为将来迁移留口子——作者已确认无需兼容旧档）；
- CreationWizard 年龄输入防 0、`canConfirm` 校验天赋点超支；`StoryLog.tsx:170` 死三元清理。

**9. 工程卫生快速赢**
- **PNG 出库（P0-4）**：`git rm --cached public/bg/*.png` + `.gitignore` 加 `public/bg/*.png`，保留 webp/svg；历史瘦身（`git filter-repo`）需与远端协作，可暂缓。
- 修文档漂移：README L50 / BACKGROUND-PROMPTS L3"PNG 优先"→"WebP→PNG→SVG"；
- scripts 接线：test-validate.ts / e2e-*.mjs / build-apk.sh 挂进 package.json；
- `build-apk.sh` 四条 `/Users/asura` 路径参数化（环境变量）；gradle `versionCode/versionName` 从 package.json 读取；
- `.npm-cache`（335MB）移出项目并 gitignore；
- 性能三件套：`persistCurrentSession` 截断 log（如只留最近 50 条）+ 写入失败降级提示；Zustand 改选择器订阅；StoryLog 历史渲染改分页/虚拟化。

### 第 3 档 · 结构性重构（中期，建议在 1~2 档之后择机）

**10. flags 类型化（B1）**：定义 `GameFlags` 接口；combat 拆为独立 `CombatState` 字段（或单键强类型）；`fate:*` 用 `Record<FateId, number>`。从根上消灭"键表示漂移"类事故（P0-2/P1-4 的根源），**建议优先做**。

**11. 合并 deltas 校验（B2）**：抽 `validateAndClamp` 纯函数库（统一别名表+钳制+增量/绝对值语义），`validateProposedStateChanges` 与 `applyDeltas` 都基于它，消除 ~200 行重复与语义分歧。

**12. 常量单源（B3）**：`STAGE_COST_BASE` 收进 `data/realms.ts` 单份，两文件 import。

**13. 拆 turn.ts**：`resolveTurn` 主干 / `settleTime+aging` / deltas 三块拆文件（694 行 → 3 个 200+ 行模块，边界已清晰）。

**14. 数值表集中（B6）**：升级价、任务奖励、战利品、好感增量、突破率锚点收进 data 配置，支持脚本化平衡测试（结合原文节奏重标定）。

**15. 死代码清理决策（B4）**：离线叙事（跟随决策 5）、卖货（跟随决策 7）、`daoHeartCheck`（接入心魔事件或删除）、未消费数据表（接入玩法或删除）。

**16. 玩法闭环（按原文优先级：双修 → 秘境 → 功法研习 → 大事件 → 结局判定）**：每项都能复用现有纯函数引擎；建议顺序——双修（多处提及零实现）→ 秘境入口（`secretRealmOpen` 已置 flag）→ 宗门大比/升仙大会/灵气潮汐（flags+指令触发）→ 结局判定（ENDINGS 接线）。

### 第 4 档 · 暂缓（作者本次未优先，建议单独排期）

- release 签名 / keystore / 版本号管理（P0-5，至少先做 versionCode 参数化）；
- Android `test/androidTest` 包名修复（P1-6，改 `com.getcapacitor.myapp` → 实际包名）；
- CI（GitHub Actions）+ lint（eslint/biome）+ 测试框架接入 `npm test`；
- e2e 去 msedge 依赖、DS_KEY 参数化；
- `allowBackup=false`、`@capacitor/*` 移入 devDependencies、`engines` 字段；
- Android 产物重新打包（现有 assets/APK 落后 ≥1 commit，测试前必须先 rebuild）。

---

## 4. 亮点（不否定整体）

- **架构分层干净**：data/engine/narrator/ui 四层边界清晰，引擎全部纯函数、不可变更新、零 React 依赖（cultivation.ts:5、systems.ts:11 的硬约束注释）——非常利于单测，同规模项目少有的水准。
- **混合引擎护栏设计是真正亮点**：`validateProposedStateChanges`（turn.ts:134）按行动语境白名单放行 AI 状态提案（location 需移动指令、mainQuest 需任务语义、数值钳 1~20），`rejectedStateChanges` 还展示"AI 想改什么、为什么被拒"（StoryLog.tsx:54-79）——既保留 AI 自由度又守住世界事实，同类 AI 游戏中的上乘设计。
- **时间口径收敛决策**：明确"AI 返回多少 timePassedMonths 就推进多少，代码不解析中文时间表述"，配 0~12 钳制与闭关代码解析例外（turn.ts:531,570-580）——A1 只是该口径下累积器的实现 bug，方向本身是对的。
- **存档/容错体系完备**：3 槽 + 30 回合自动 + 每回合现场会话 + 突破/战斗前快照回退 + JSON 导入导出 + 多标签接管（storage 事件）+ ErrorBoundary 清理损坏数据重载。
- **数据转写质量高**：每个数据文件标注原文章节，原文未给的数值注明"推算/取舍"（GROWTH_INTERPOLATED、突破率锚点插值、洞府升级价 4 倍递增）；`nextId`/`normalizeLog` 对历史 id 冲突的处理体现真实踩坑后的严谨。
- **宣纸设计系统完整落地**：index.css 设计令牌 + theme.ts 场景映射复刻原文 4.5 配色，Panel/Bar/Chip/Tag 与 LaTeX 面板一一对应；`scripts/gen-bg/verify-bg/opt-bg` 素材管线（固定种子可复现 + 像素校验）完整。
- **工程底线干净**：vite base 相对路径处理得当、分层 gitignore 无泄漏、smoke 31 + test-validate 30 项断言扎实、README 167 行详尽、零密钥入库。

---

## 5. 总评

这是一个**架构意识明显高于平均水准的中型游戏项目**：分层、护栏、存档、测试脚本都可圈可点。主要问题集中在三处：**跨模块的"表示漂移"**（injury id/中文名、`'炼丹'`/`'lian-dan'`、flags 魔法键——P0-2/P1-4 的直接根源）、**几处核心数值/流程硬伤**（衰老结算 P0-1、修炼 10× 通胀 P0-3、逆天改命死链 P1-1、突破三途无差异 P1-3）、**工程卫生**（80MB PNG 入库、release 不可发布、文档漂移）。修复优先级建议按 **P0-1 → P0-2 → P0-3 → P1-1** 推进，并借机完成 flags 类型化与 deltas 校验合并两项重构——这两项能从根上杜绝后续同类事故。

---

## 6. 实施记录（第 1 档 · 已完成 2025-08-30）

按作者确认的决策（低成本快速赢、核心体验以文档为准、存档无需兼容旧档但留版本号口子），第 1 档 4 项核心玩法修复已落地并全量回归通过：

| # | 修复项 | 改动文件 | 验证（smoke 新增断言） |
|---|---|---|---|
| 1 | **衰老/寿元结算**（P0-1）：`ageMonths` 由「只留小数余数」改为「留不足一年的余数」，整年折算入年龄/寿元，「坐化」机制恢复 | turn.ts（applyAging + 两处注释） | 3b：12 月年龄 +1、寿元 −1；24 月 +2；寿元尽 → 坐化 |
| 2 | **injury 统一存 id**（P0-2）：4 个中文名写入点改 id（systems.ts:261/427/439、economy.ts:316），疗伤降级链改 id（systems.ts:562/565），战斗压制改 id 判断（combat.ts:316-317），heal 的 `injuryIdOf` 兼容 id/中文名（历史档兜底）；state.ts 注释同步 | systems.ts / economy.ts / combat.ts / state.ts | 7b：id 重伤可寻医治愈、闭关降级为轻伤、战斗压制 ×0.8 生效；3：心魔闭关 ×1、内伤 −50% 生效 |
| 3 | **修炼公式去 10× 通胀**（P0-3）：去掉与成长基数（炼气=10）重复的字面量 `10`，测试配置炼气月修为 480 → 48 | cultivation.ts | 3：量级断言 20~80 防回归 |
| 4 | **逆天改命接线**（P1-1）：`majorBreakthrough` 成功返回 `fateChoices` 三选一 → actions 展示「承 · 名称」选项 → `routeCommand` 新增 `fateChange` 指令 → `applyFateChange` 落地 `flags.fate:*` | breakthrough.ts / actions.ts | 6b：候选 3 项、路由正确、flags 计数 +1 |

**回归结果**：`tsc -b` 0 错误 ｜ `npm run smoke` **44/44**（修复前 31/31）｜ `scripts/test-validate.ts` 31/31 ｜ `npm run build`（vite）通过。

**说明**：
- 修炼节奏仅修掉了 10× 双乘这一硬伤；如需进一步向原文「炼气 3~10 年」收紧，属数值平衡微调（GROWTH_BASE / cultMax），建议并入第 3 档「数值表集中」统一调参。
- 存档未做迁移（按作者决策「无需兼容旧档」）；旧档中的中文名伤势仍可被 `injuryIdOf` 兜底识别。存档 schema 版本号 bump 留给第 2 档统一处理。
- 剩余第 2 档（LLM 诚实降级、键对齐、商品闭环、PNG 出库等）与第 3 档（flags 类型化、deltas 校验合并）按原清单继续。

---

## 7. 实施记录（第 2 档 · 已完成 2025-08-30）

第 2 档低成本快速赢已全部落地，按作者确认的优先级（玩法正确性 → 性能 → 工程卫生）：

| 类别 | 修复项 | 改动文件 |
|---|---|---|
| LLM 诚实降级 | 无 Key 时系统指令走代码结算（普通修炼按 1 月结算、显式闭关按 N 月、其余按指令既定流逝）；自由行动仍拦截但提示明确；SettingsPanel / README / state 文案同步修正；删除死代码 offline.ts | turn.ts / SettingsPanel.tsx / README.md / state.ts / offline.ts（删除） |
| 数据键对齐 | 创角「药理通神」天赋写 `'lian-dan'`（修复 `'炼丹'` 键漂移）；`applyDeltas` 背包键经 `itemIdOf` 反查映射回 id（AI 中文名→id）；heal 丹药消耗改 id（买的聚气丹可治愈伤势） | CreationWizard.tsx / turn.ts / economy.ts |
| 商品闭环 | 筑基丹人道破境消耗 +15% 成功率；新增「研习」指令（`studyGongfa`：消耗卷轴、悟性判定、习得对应品级 GONGFAS）；「摆摊卖货 / 卖 X」接线 `marketSell`；战斗用丹校验背包并消耗 | breakthrough.ts / economy.ts / actions.ts |
| 细节修复 | `travelLike` 正则补「探索/探秘」；声望 bonus 落地 `flags.fame` 并在状态卡展示（不再静默丢弃）；SavePanel 读档/导入走 `continueFromSave` 归一化；存档 schema 升 v3；年龄输入校验 + canConfirm 双保险；StoryLog 死三元 / any 清理 | turn.ts / CreationWizard.tsx / StatusCard.tsx / SavePanel.tsx / save.ts / StoryLog.tsx |
| 性能 | 现场会话剧情流上限 200→80（每回合写入更轻）；`saveSession` 配额不足时降级保留 30 条；StoryLog / CommandBar 改 Zustand 选择器订阅；历史弹窗只渲染最近 60 回合 | session.ts / store.ts / StoryLog.tsx / CommandBar.tsx |
| 工程卫生 | 36 张 PNG（80MB）`git rm --cached` + .gitignore（仓库瘦身；历史瘦身可选 filter-repo）；README / BACKGROUND-PROMPTS 文档漂移修正（WebP→PNG→SVG）；scripts 全部挂进 package.json（含 `npm test`）；build-apk.sh 路径参数化（环境变量覆盖）；APK versionName 读 package.json；删除 .npm-cache（335MB） | .gitignore / README.md / BACKGROUND-PROMPTS.md / package.json / build-apk.sh / build.gradle |

**回归结果**：`tsc -b` 0 错误 ｜ `npm run smoke` **56/56**（第 1 档后 44/44）｜ `npm run test:validate` 31/31 ｜ `npm run build` 通过；e2e-llm 场景 B 断言同步更新，新增场景 C（无 Key 系统指令可玩）。

**说明**：第 3 档结构重构（flags 类型化 / deltas 校验合并 / turn.ts 拆分）与更多玩法闭环（双修 / 秘境 / 大事件 / 结局判定）按原清单继续排期。

---

## 8. 实施记录（第 3 档 · 已完成 2025-08-30）

第 3 档结构性重构 + 玩法闭环已全部落地：

### 结构性重构

| 项 | 改动 |
|---|---|
| **B1 flags 类型化** | state.ts 新增 `GameFlags` 接口：ageMonths / breakCooldown / hiddenInjury / modao / spiritBeast / secretRealmOpen / combat / fame / dead / location / openingScript / heartStoneUsed / lastBreakFailed / combatLost 等已知键全部类型化 + 索引签名兜底（fate:* 与扩展键）；`GameState.flags` 由 `Record<string, string\|number\|boolean>` 升级，键拼写错误从此有编译期保护 |
| **B2 deltas 合并** | 新建 `engine/deltas.ts`：`DELTA_ALIASES` / `DELTA_LABELS` / `RANGED_FIELDS` / `toNumber` / `clampNum` / `normalizeMood` 单源，`validateProposedStateChanges` 与 `applyDeltas` 共用（心境归一化两处重复消除）；turn.ts 经 re-export 保持旧导入路径兼容 |
| **B3 常量单源** | `STAGE_COST_BASE` + `stageCostOf` 收进 `data/realms.ts` 单份，cultivation / breakthrough 共用（删除双份手工同步表） |
| **拆 turn.ts** | 694 → ~310 行：`applyAging` / `reconcileLifespan` 移入 `engine/time.ts`，deltas 校验/落地移入 `engine/deltas.ts`；store / smoke 等旧导入经 turn.ts re-export 兼容 |
| **B6 数值表集中** | 新建 `data/balance.ts`：`CAVE_UPGRADE_COSTS`（洞府 200/800/3000/12000）、`SECT_TASK_REWARD`（宗门任务奖励）、`COMBAT_LOOT`（战斗战利品公式） |
| **daoHeartCheck 接线** | turn.ts 月度回合接入（时间流逝且未死亡时）：道心<40 心魔渐生、心魔缠身道心达标可自渡、心如磐石挡劫——此前悬空的死代码激活 |

### 玩法闭环

| 项 | 改动 |
|---|---|
| **结局判定** | DeathOverlay 按 `flags.dead` / `flags.modao` 映射 ENDINGS（坐化→zuohua、入魔→rumo-line、战死/渡劫陨落→yunluo），显示结局名 / 类型 / 描述 |
| **秘境** | 随机事件「秘境入口现世」置位 `secretRealmOpen` 后，新增「探索秘境」指令（路由须先于游历规则）→ `secretRealmExplore`：灵石 50% / 灵药 25% / 功法卷轴 15% / 妖兽负伤 10%，一次性消耗标记 |
| **大事件** | `majorEventRoll` 按入道年份周期触发（灵气潮汐千年 / 猎魔大会 20 年 / 宗门大比 10 年 / 升仙大会 5 年 + 天机拍卖会 2% 不定期），接入游历并附小额奖励 |
| **双修** | 情缘新增「双修」行动：需道侣 + 好感 80+，修为进益（玄阴/纯阳体 ×1.5）+ 心境 1.2；情缘面板展示双修选项 |

**回归**：`tsc -b` 0 错误 ｜ `npm run smoke` **62/62**（第 2 档后 56/56）｜ `npm run test:validate` 31/31 ｜ `npm run build` ✓。

**说明**：大事件完整玩法（参赛/拍卖/夺魁）、飞升结局判定（登仙圆满 + 渡劫成功）、双修更多增益（纯阳/玄阴后续深化）留待后续迭代；所有新系统均挂进 smoke 断言防回归。

---

## 9. LLM 叙事模块审计与修复（2025-08-30）

审计对象：`src/game/narrator/llm.ts`（412 行）+ `balance.ts` + `worldview.ts` + 调用链（turn / store / deltas）。作者确认的决策：评估报告 + 修复清单 ｜ 重点 = 逻辑健壮性 / 提示词质量 / 安全注入 / 成本性能 ｜ 低成本快速赢优先 ｜ 叙事限 300~800 字 + max_tokens 8000 ｜ 提案语义取「增量明确化」。

### 9.1 问题清单（按严重度）

**P0**
- **P0-1 负增量提案被校验层钳死（数据正确性）**：提示词教 AI「正加负减」（llm.ts:173/179），但 `validateProposedStateChanges` 先把负数钳成 0/1（deltas.ts:148/162/134），`applyDeltas` 又跳过 0 值（deltas.ts:232）→ **道具消耗静默丢失、属性 −1 反而 +1**，affinity/bag/stats/enlightenment/technique 五类字段全中招。
- **P0-2 上下文无 token 预算**：历史全量回传（系统指令路径 80 条 vs 自由行动 24 条，不对称，turn.ts:167 vs llm.ts:382/387）、narrative 无上限 → 长局必然上下文溢出/成本爆炸；无任何截断保护。

**P1**
- **P1-1 sanitizeOptions 类型崩溃**：非字符串 text 直接 `.trim()` → TypeError 废回合；字符串数组项被全丢 → 空选项。
- **P1-2 narrateOpening 竞态**：开局 AI 异步未返回时玩家可推进，返回后原位覆盖第一卡 + 覆盖后续回合选项（store.ts:145-175）。
- **P1-3 间接注入面**：AI 叙事末 300 字原文进 system prompt（最高权限面，turn.ts:113）；玩家输入裸进 user 消息且双写（store.ts:275 + llm.ts:308）；无指令层级声明。
- **P1-4 提案语义歧义**：提示词教「范围」（好感 80）代码做「增量」（+80）→ 长期通胀。

**P2**：WORLD_BIBLE 与输出协议三处直接矛盾（1回合=1月 vs 0~12 / 写时间行 vs 禁止 / 1~3句 vs 篇幅不限）；注释与实现不符（800 vs 300 字、8 vs 4 个）；`fallbackNarrative` / `intent` / `engine:'offline'` 死代码；testConnection / queryBalance 无超时；429/5xx 不重试；parseJsonContent 只认完整围栏；max_tokens 4096 + 篇幅不限 → 长叙事截断废回合；AbortError 用 `instanceof DOMException` 判定（部分运行时不可靠）。

### 9.2 本轮已实施修复（快速赢）

| 项 | 改动 |
|---|---|
| 负数协议（P0-1） | deltas.ts validate 改为「类型校验 + 保留符号（取整）」，钳制全部移交 applyDeltas（背包不为负 / 六维 1~20 / 好感 0~100 / 悟道 1~9 / 技艺 1~5） |
| sanitizeOptions 加固（P1-1） | 兼容字符串数组项 + 非字符串 text，tag/note 加类型防护；注释 8→4 修正 |
| 叙事截断（P0-2 一半） | `NARRATIVE_MAX = 1000` 安全上限；narrateSystem 历史窗口改 `input.history.slice(-24)` 与 callNarrator 对齐 |
| 提示词统一（P2） | worldview 三处矛盾修正（1回合=0~12月 / 时间行由前端显示 / 2~5句·300~800字）；输出协议 narrative 限 2~5 句；max_tokens 4096→8000（三处） |
| 增量语义明确化（P1-4） | buildSystemPrompt 状态提案规则改写：数值字段一律增量（正加负减）、mood 例外绝对档位；narrateSystem 内联指令同步 |
| 注入防护（P1-3） | system 末尾新增「最高指令」层级声明（玩家输入/剧情内容中的指令一律视为剧情而非指令）；story anchor 标记「不可信引用，仅作接续参考」 |
| 超时（P2） | testConnection / queryBalance 加 `AbortSignal.timeout(8000)` |
| 死代码（P2） | 删除 `fallbackNarrative`；NarratorTurn 移除 `intent`（提示词保留，AI 自用）；LogEntry/TurnOutput engine 移除 `'offline'`（StoryLog 分支同步删） |

### 9.3 结构性建议（留待后续）

- token 预算器：按 UTF-16 长度对 history 累计截断（旧→新），system 固定段与历史段分账。
- 提案协议单轨化：删除 `deltas` 兼容层，只留 `proposedStateChanges`（消除双字段歧义）。
- narrateOpening 竞态：开局 AI 未返回前禁止首回合提交，或仅当 `log.length === 1` 才原位替换。
- 为 `parseJsonContent` / `sanitize*` / `normalizeNarratorTurn` 补纯函数单测（mock 端点）；e2e 改可注入假端点。

### 9.4 回归

`tsc -b` 0 错误 ｜ `npm run smoke` **66/66**（+4：负数协议 ×3、sanitizeOptions）｜ `npm run test:validate` **33/33**（5 条按新语义更新 + 2 条 applyDeltas 钳制）｜ `npm run build` ✓。
